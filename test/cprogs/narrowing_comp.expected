Not supported yet: top level item
Prep_incl (
  (
    "#include"
    System_lib_str "<Rinternals.h>"
    "\n"
  )
)
larger_than_3: ∀'a,'b. ('a & int1) -> 'a & int1

size_bound: ∀'a,'b. ('a & vec[int(i('b & (..10)))]) -> 'a & vec[int(i('b & (..10)))]

